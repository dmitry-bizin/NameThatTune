<?xml version="1.0" encoding="UTF-8"?>
<?language javascript?>

<?import javafx.scene.control.*?>
<?import javafx.scene.*?>
<?import javafx.scene.layout.Pane?>
<?import javafx.stage.Screen?>
<Pane xmlns:fx="http://javafx.com/fxml"
      xmlns="http://javafx.com/javafx"
      fx:controller="controller.SettingsController"
      stylesheets="@../css/style.css"
      styleClass="settingsBackground"
      fx:id="pane">

    <fx:define>
        <Screen fx:factory="getPrimary" fx:id="screen"/>
    </fx:define>

    <Label fx:id="round1SettingsLabel" text="Настройки 1го тура" styleClass="settingsLabel"
           onMouseClicked="#round1SettingsLabelClick">
        <fx:script>
            round1SettingsLabel.layoutX = 0.03 * screen.visualBounds.width;
            round1SettingsLabel.layoutY = 0.1 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="round2SettingsLabel" text="Настройки 2го тура" styleClass="settingsLabel"
           onMouseClicked="#round2SettingsLabelClick">
        <fx:script>
            round2SettingsLabel.layoutX = 0.03 * screen.visualBounds.width;
            round2SettingsLabel.layoutY = 0.2 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="round3SettingsLabel" text="Настройки 3го тура" styleClass="settingsLabel"
           onMouseClicked="#round3SettingsLabelClick">
        <fx:script>
            round3SettingsLabel.layoutX = 0.03 * screen.visualBounds.width;
            round3SettingsLabel.layoutY = 0.3 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="supergameSettingsLabel" text="Настройки суперигры" styleClass="settingsLabel"
           onMouseClicked="#supergameSettingsLabelClick">
        <fx:script>
            supergameSettingsLabel.layoutX = 0.03 * screen.visualBounds.width;
            supergameSettingsLabel.layoutY = 0.4 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="currentDirectorySettings" text="Выбрать директорию с мелодиями" styleClass="settingsLabel"
           onMouseClicked="#currentDirectorySettingsClick">
        <fx:script>
            currentDirectorySettings.layoutX = 0.03 * screen.visualBounds.width;
            currentDirectorySettings.layoutY = 0.5 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="backLabel" text="Назад" styleClass="settingsLabel" onMouseClicked="#backLabelClick">
        <fx:script>
            backLabel.layoutX = 0.03 * screen.visualBounds.width;
            backLabel.layoutY = 0.6 * screen.visualBounds.height;
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

</Pane>
