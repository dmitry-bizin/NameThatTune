<?xml version="1.0" encoding="UTF-8"?>
<?language javascript?>

<?import javafx.scene.control.Label?>
<?import javafx.scene.Cursor?>
<?import javafx.scene.layout.Pane?>
<?import javafx.stage.Screen?>
<Pane xmlns:fx="http://javafx.com/fxml/1"
      stylesheets="@../css/style.css" styleClass="mainBackground"
      xmlns="http://javafx.com/javafx/8.0.172-ea"
      fx:controller="controller.MainController" fx:id="pane">

    <fx:define>
        <Screen fx:factory="getPrimary" fx:id="screen"/>
    </fx:define>

    <Label fx:id="gameLabel" text="Игра"
           styleClass="mainLabel" onMouseClicked="#gameLabelClick">
        <fx:script>
            gameLabel.setLayoutX(0.6 * screen.getVisualBounds().getWidth());
            gameLabel.setLayoutY(0.3 * screen.getVisualBounds().getHeight());
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="settingsLabel" text="Настройки"
           styleClass="mainLabel" onMouseClicked="#settingsLabelClick">
        <fx:script>
            settingsLabel.setLayoutX(0.6 * screen.getVisualBounds().getWidth());
            settingsLabel.setLayoutY(0.4 * screen.getVisualBounds().getHeight());
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

    <Label fx:id="exitLabel" text="Выход"
           styleClass="mainLabel" onMouseClicked="#exitLabelClick">
        <fx:script>
            exitLabel.setLayoutX(0.6 * screen.getVisualBounds().getWidth());
            exitLabel.setLayoutY(0.5 * screen.getVisualBounds().getHeight());
             </fx:script>
        <cursor>
            <Cursor fx:constant="HAND"/>
        </cursor>
    </Label>

</Pane>
